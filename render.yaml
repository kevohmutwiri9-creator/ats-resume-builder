# Render.com Static Site Configuration
services:
  - type: web
    name: ats-resume-builder
    env: static
    rootDir: .
    buildCommand: ""
    staticPublishPath: .
    
# Environment variables
envVars:
  - key: NODE_ENV
    value: production
